# legacy-vite（已归档）

本目录为 **Umi 重构前** 的 Vite 8 + JavaScript 实现，仅作 API 语义与 UI parity 参考。

- **生产入口**：仓库根目录 `@umijs/max` 工程（`harness dev` / `harness build`）
- **勿再扩展**本目录功能；新需求一律在 `src/` Umi 工程实现
- 本地对照运行：`npm run legacy:vite:dev`（需单独配置代理）

归档日期：2026-06-03（`frontend-umi-refactor`）
